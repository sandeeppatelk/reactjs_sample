#!/usr/bin/env node

var gulp = require('gulp');
var gulpBabel = require('gulp-babel');

gulp.task('build', function() {
  return gulp.src('./gulp-app.js')
      .pipe(gulpBabel({
        presets: ['es2015']
      }))
      .pipe(gulp.dest('bin/'));
});

gulp.task('default', ['build']);
